requirements for 1.0
- integrate libEmu for shellcode analysis
  + understand what the shellcode does, akin to nepenthes/mwcollect
- be able to inspect spidermonkey variables (ie heapspray) from honeyclient
  + will require better spidermonkey bindings
  + will require spidermonkey instrumentation
    + ie 'list all local variables', 'inspect variable FOO'
- database integration
  + queue
  + results
- proxy integration
  + Tor
  + generic proxies
- handle POSTs in <form /> stuff ..
  + what data to submit?
- finish up VB analysis
  + make it on par with the JS detection
  + vuln module support
- more alerting modules
  + snort
  + heuristics (ie multiple decodes)
  + shellcode detection via libEmu


DATABASE schema thoughts
- use SQLObect so it's backend agnostic
- Queue table
  URL - string 
  priority - int
- URL table
  URL - string
- Aert table
  url_id - ForeignKey(URL)
  referrer - ForeignKey(URL)
  source - string (ie 'clamav', 'vuln_module')
  message - string (ie 'HTML.Exploit-Gen4')
  timestame - TimeDateCol()